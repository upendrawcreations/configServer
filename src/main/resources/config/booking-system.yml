server:
  port: 8086

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/Layout?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: rootroot
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.MySQLDialect
  kafka:
    bootstrap-servers: localhost:9092

app:
  jwt:
    secret: "change-this-secret-to-a-long-random-string"
  kafka:
    booking-alert-topic: booking-alerts-topic
